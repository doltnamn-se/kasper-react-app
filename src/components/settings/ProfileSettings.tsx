import { useLanguage } from "@/contexts/LanguageContext";

export const ProfileSettings = () => {
  const { t } = useLanguage();

  return (
    <div className="space-y-4">
      {/* Profile settings content will be added later */}
    </div>
  );
};