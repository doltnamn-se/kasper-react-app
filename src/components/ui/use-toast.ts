
// Re-export the useToast and toast functions from the hooks implementation
import { useToast, toast } from "@/hooks/use-toast";

// Export both functions for use throughout the application
export { useToast, toast };
